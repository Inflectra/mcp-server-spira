"""
Main entry point for running the MCP Inflectra SpiraPlan server as a module.

This allows running the server with:
    python -m mcp_server_spiraplan
"""
from mcp_server_spiraplan.server import main

if __name__ == "__main__":
    main()